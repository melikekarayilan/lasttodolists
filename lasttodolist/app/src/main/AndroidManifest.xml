<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.melikekarayilan.lasttodolist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">

        </activity>

        <activity android:name=".Activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.RegisterActivity">

        </activity>
        <activity android:name=".Activities.ToDoListActivity">

        </activity>
        <activity android:name=".Activities.TodoListItemsActivity"></activity>
        <activity android:name=".Activities.ItemDetailActivity"></activity>
        <activity android:name=".Activities.AddNewItemActivity"></activity>
        <activity android:name=".Activities.AddNewTodolist"></activity>
        <activity android:name=".Activities.UpdateItemActivity"></activity>
        <activity android:name=".Activities.UpdateListNameActivity"></activity>
    </application>

</manifest>